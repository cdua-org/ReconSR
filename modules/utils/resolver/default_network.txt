[Options]

### General Network & Retry Options ###

# General timeout for network dial operations, in seconds (default: 10)
Timeout=10

# TCP keep-alive interval for persistent connections, in seconds (default: 30)
KeepAlive=30

# Base pause between HTTP retry attempts across all modules, in seconds (default: 2)
RetryBaseDelay=2


### DNS Options ###

# Timeout for simple DoH-only queries (LOC, SSHFP, DNSKEY, etc.), in seconds (default: 10)
DNSQueryTimeout=10

# Timeout for DoH + Plain DNS fallback queries (NS, MX, TXT, etc.), in seconds (default: 15)
DNSFallbackTimeout=15

# Timeout for concurrent brute-force DNS operations (DKIM, SRV, TLSA), in seconds (default: 40)
DNSBruteTimeout=40

# Maximum parallel goroutines for brute-force DNS handlers (default: 10)
DNSConcurrency=10

# Maximum retry attempts for DNS resolution (default: 3)
MaxRetriesDNS=3


### HTTP & General API Options ###

# Timeout for HTTP API requests (RIPE, Shodan, VirusTotal, etc.), in seconds (default: 30)
HTTPTimeout=30

# Maximum retry attempts for HackerTarget API requests (default: 3)
MaxRetriesHT=3


### Certificate & crt.sh Options ###

# Disable Certspotter certificate transparency lookups (default: false)
DisableCertspotter=false

# When false, expired-cert subdomains are aggregated into a summary property on the root domain;
# when true, each expired-cert subdomain is emitted as an individual node into the system (default: false)
DisableCertExpiredSubdomains=false

# Disable direct PostgreSQL queries to crt.sh (default: false)
DisableCrtshPG=false

# Timeout for crt.sh direct PostgreSQL connections, in seconds (default: 30)
CrtshPGTimeout=30

# Maximum retry attempts for certificate transparency lookups (default: 3)
MaxRetriesCert=3


### WHOIS, RDAP & ASN Metadata Options ###

# Disable RDAP protocol and fall back to legacy WHOIS over port 43 (default: false)
DisableRDAP=false

# Maximum retry attempts for WHOIS/RDAP queries (default: 3)
MaxRetriesWhois=3

# HTTP timeout for ASN metadata API lookups, in seconds (default: 30)
TimeoutASNMeta=30

# Maximum retry attempts for ASN metadata lookups (default: 3)
MaxRetriesASNMeta=3

# Maximum depth for ASN transit chain traversal (default: 3)
MaxRecursionDepth=3


### IP Metadata Options ###

# Maximum retry attempts for IP metadata lookups (default: 3)
MaxRetriesIPMeta=3


### Mailcrypto Options ###

# Prevent domains and subdomains from being routed to the mailcrypto brute-force handler (default: true)
DisableMailcryptoBruteForce=true


### Anubis Options ###

# Maximum number of subdomains to process from jldc.me/Anubis (default: 1000)
AnubisLimit=1000


### VirusTotal Options ###

# Enable routing of subdomains to the VirusTotal domain endpoint in addition to root domains (default: false)
VirustotalScanSubdomains=false

# Maximum number of pages to fetch for VirusTotal lookups; each page fetches up to 40 objects (default: 5)
VirustotalMaxPages=5


### Shodan Options ###

# Enable routing of subdomains to the Shodan domain endpoint in addition to root domains; each lookup costs 1 query credit (default: false)
ShodanScanSubdomains=false

# Include historical DNS data in Shodan domain lookup results (default: false)
ShodanDomainHistory=false

# Filter Shodan domain results by DNS type; possible values: A, AAAA, CNAME, NS, SOA, MX, TXT (default: empty, returns all)
ShodanDomainType=

# Maximum number of pages to fetch for Shodan domain lookups; each page costs 1 query credit (default: 1)
ShodanMaxDomainPages=1

# Include all historical banners in Shodan IP lookup results (default: false)
ShodanIPHistory=false

# Return only ports and general host information without full banners for Shodan IP lookups (default: false)
ShodanIPMinify=false

### CIRCL Options ###

# Include metadata for CIRCL vulnerabilities (default: true)
CirclWithMeta=true

# Include linked vulnerabilities for CIRCL vulnerabilities (default: false)
CirclWithLinked=false

# Include comments for CIRCL vulnerabilities (default: false)
CirclWithComments=false

# Include bundles for CIRCL vulnerabilities (default: false)
CirclWithBundles=false

# Include sightings for CIRCL vulnerabilities (default: false)
CirclWithSightings=false

# Maximum retry attempts for CIRCL API requests (default: 3)
MaxRetriesCircl=3

[DoH]
https://dns.cloudflare.com/dns-query
https://dns.google/resolve
https://unfiltered.adguard-dns.com/resolve
https://mozilla.cloudflare-dns.com/dns-query
https://doh.dns.sb/dns-query

[Plain]
1.1.1.1
8.8.8.8
9.9.9.10
94.140.14.140
84.200.69.80
193.183.98.154
4.2.2.1
1.0.0.1
8.8.4.4
149.112.112.10
94.140.14.141
84.200.70.40
185.121.177.177
4.2.2.2
